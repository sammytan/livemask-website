name: Runner Probe

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on:
      group: livemask-ci
      labels: [self-hosted, linux, livemask, docker, ci]
    steps:
      - run: |
          hostname
          whoami
          pwd
          docker --version
