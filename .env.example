# API base URL for the LiveMask backend
# VITE_API_BASE_URL=http://localhost:8000

# Mock mode: set to "false" or "0" to call real Backend APIs
# Defaults to "true" (mock mode) when unset
# VITE_API_MOCK_MODE=true
